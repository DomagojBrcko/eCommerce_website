import React from "react";
import { StyledTitle } from "./Title.styled";

export default function Title() {
    return (
        <StyledTitle>
            <h1>No Boundaries.</h1>
            <h1>Zero limits.</h1>
        </StyledTitle>
    );
}
